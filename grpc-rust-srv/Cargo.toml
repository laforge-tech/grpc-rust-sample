[package]
name = "grpc-rust-srv"
version = "0.1.0"
edition = "2021"

[dependencies]
grpc-rust-api = { path = "../grpc-rust-api" }
tonic = "0.9"
prost = "0.11"

tokio = { version = "1.0", features = ["macros", "rt-multi-thread"] }
